/*
 * The callout is styled entirely in CSS. This module exists because loadBlock
 * imports {block}.js unconditionally and console.errors when the import fails, and
 * a callout is on 110 pages.
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {}
